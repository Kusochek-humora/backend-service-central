name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p ~/backups
            cd ~/backend-service-central
            git pull
            printf '%s\n' "$ENV_FILE" > .env
            sudo ufw allow 3000/tcp
            sudo docker-compose exec -T db pg_dump -U postgres standup_db > ~/backups/backup_$(date +%Y%m%d_%H%M%S).sql || true
            sudo docker-compose down
            sudo docker-compose up -d --build
            sleep 10
            sudo docker-compose exec -T app npm run migration:run:prod
            sudo docker-compose exec -T app node dist/db/seeds/create-chief-editor.js
            sudo docker-compose logs app
          envs: ENV_FILE
        env:
          ENV_FILE: ${{ secrets.ENV_FILE }}
